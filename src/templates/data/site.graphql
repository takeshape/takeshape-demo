query {
  settings: getSiteSettings {
    siteTitle
    copyright
  }
  books: getBookList {
    total
    items {
      _contentTypeName
      _enabledAt
      title
      author {
        name
      }
      coverImage {
        s3Key
      }
    }
  }
}
